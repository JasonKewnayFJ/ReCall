<Window x:Class="ReCall___.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:ReCall___"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Title="MainWindow" MaxHeight="560" MinWidth="400" Height="6000" Width="450"> 
    <Border CornerRadius="10" Background="#B04B37" Padding="10" MouseDown="Border_MouseDown" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Grid.RowSpan="2" VerticalAlignment="Center" >
                <TextBlock Text="ReCall" Foreground="White" FontFamily="Consolas" FontSize="30" Margin="10 0 0 0" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="3" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="5" >
                <Border CornerRadius="10" Background="#E0A363">

                    <Button Content="▾" Click="CloseWindow" x:Name="HideBTN" Background="Transparent" Foreground="Black" BorderBrush="black" BorderThickness="0"/>
                </Border>



                <Border CornerRadius="10" Background="#E0A363" Margin="5 0 0 0">
                    <Button Content="🞬" Click="ABSOLUTEClose" x:Name="CloseBTN"  Background="Transparent" Foreground="Black" BorderBrush="black" BorderThickness="0"/>
                </Border>

            </StackPanel>



            <Border Grid.Row="2" Grid.RowSpan="2" Grid.Column="0" Grid.ColumnSpan="2" Background="#E0A363" CornerRadius="10" Margin="5" Padding="10">
            <StackPanel>
                    <Border CornerRadius="10" Background="White" Padding="5" Margin="0 0 0 3">
                        <StackPanel>
                            <TextBlock Text="Нынешняя запись в Буфере:" HorizontalAlignment="Left" TextAlignment="Left" Background="Transparent"/>
                            <Separator Background="Black"/>
                            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                <TextBlock x:Name="Note" Text="{Binding CurrentBoardNote}"  HorizontalAlignment="Left" TextAlignment="Left" Background="Transparent" Height="50" />
                                
                            </ScrollViewer>
                            
                        </StackPanel>

                    </Border>
                    <Border CornerRadius="10" Background="White" Padding="5" Margin="0 0 0 3">
                        <StackPanel>
                            <TextBlock Text="Предыдущая запись в Буфере:"  HorizontalAlignment="Stretch" TextAlignment="Left" Background="Transparent"/>
                            <Separator Background="Black"/>
                            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                <TextBlock x:Name="PrevNote" Text="{Binding PreviousBoardNote}" HorizontalAlignment="Left" TextAlignment="Left" Background="Transparent"  Height="50" />
                            </ScrollViewer>
                            
                        </StackPanel>

                    </Border>
                
                    <Border CornerRadius="10" Background="White" Padding="5" Margin="0 0 0 3">


                        <ListBox ItemsSource="{Binding NotesList}" Height="280" HorizontalContentAlignment="Left" ScrollViewer.CanContentScroll="True" >
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="Margin" Value="2" />
                                    <Setter Property="Padding" Value="10" />
                                    <Setter Property="MaxHeight" Value="70" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="Bd"
                                                Background="#E0A363"
                                                CornerRadius="10"
                                                BorderThickness="1"
                                                BorderBrush="Transparent"
                                                Padding="{TemplateBinding Padding}"
                                                SnapsToDevicePixels="true">
                                                                    <ContentPresenter 
                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Bd" Property="Background" Value="#B04B37" />
                                                        <Setter Property="Foreground" Value="White" />
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Bd" Property="Background" Value="#B04B37" />
                                                        <Setter Property="Foreground" Value="White" />
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter Property="Foreground" Value="Gray" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <EventSetter Event="MouseLeftButtonUp" Handler="ListBoxItem_MouseLeftButtonUp" />
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>


                    </Border>
                </StackPanel>
                
            </Border>
        </Grid>

    </Border>
</Window>
